# Included by compose.yaml, also usable independently without application .env.
services:
  cassandra:
    image: ${CARTYX_CASSANDRA_IMAGE:-ghcr.io/biozal/cartyx-cassandra@sha256:a8705b2f7660013a90eeba260521fe640d320da91fdf11c7dcd94ab3f9f0ebd8}
    entrypoint: [bash, /config/cassandra-start.sh]
    environment:
      CASSANDRA_CLUSTER_NAME: cartyx-local
      CASSANDRA_DC: dc1
      CASSANDRA_ENDPOINT_SNITCH: GossipingPropertyFileSnitch
      CASSANDRA_HOST: localhost
      MAX_HEAP_SIZE: 1G
      HEAP_NEWSIZE: 256M
    volumes:
      - cassandra-data:/var/lib/cassandra
      - ../charts/cartyx-data/files:/config:ro
      - ../../.local/data/local:/secrets:ro
    # CQL is internal only. Use exec for administration; no default-login window
    # is exposed on the host during initial provisioning.
    healthcheck:
      test: [CMD, python3, /config/cassandra-admin.py, ready]
      interval: 30s
      timeout: 35s
      retries: 5
      start_period: 180s
    mem_limit: 3g
    cpus: 2
    restart: unless-stopped

  cassandra-init:
    image: ${CARTYX_CASSANDRA_IMAGE:-ghcr.io/biozal/cartyx-cassandra@sha256:a8705b2f7660013a90eeba260521fe640d320da91fdf11c7dcd94ab3f9f0ebd8}
    entrypoint: [python3, /config/cassandra-admin.py, bootstrap]
    environment:
      CASSANDRA_HOST: cassandra
    volumes:
      - ../charts/cartyx-data/files:/config:ro
      - ../../.local/data/local:/secrets:ro
    depends_on:
      cassandra:
        condition: service_started
    mem_limit: 256m
    restart: 'no'

  janusgraph:
    image: ${CARTYX_JANUSGRAPH_IMAGE:-ghcr.io/biozal/cartyx-janusgraph@sha256:ebd0c068c444e2f8685117bb9cd104687304edb8553a1d575c8e493e8bfb849b}
    entrypoint: [bash, /config/janusgraph-start.sh]
    environment:
      CASSANDRA_HOST: cassandra
    ports:
      - '127.0.0.1:${GREMLIN_LOCAL_PORT:-18182}:8182'
    volumes:
      - ../charts/cartyx-data/files:/config:ro
      - ../../.local/data/local:/secrets:ro
    depends_on:
      cassandra-init:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          CMD-SHELL,
          'java -Xms32m -Xmx128m -cp "lib/*" groovy.ui.GroovyMain /config/graph-probe.groovy >/dev/null 2>&1',
        ]
      interval: 60s
      timeout: 30s
      retries: 3
      start_period: 120s
    mem_limit: 2g
    cpus: 2
    restart: unless-stopped

volumes:
  cassandra-data:
