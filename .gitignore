# Never commit credentials. Secrets are created out-of-band; see README.
secrets/
*.key
*.pem
*-token
kubeconfig

# Local credentials, database archives, and tool dependencies
.local/
node_modules/
__pycache__/
