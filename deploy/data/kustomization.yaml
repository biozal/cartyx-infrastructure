apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - backup-resources.yaml
configMapGenerator:
  - name: cartyx-data-backup-code
    files:
      - package.json
      - package-lock.json
      - cluster-api.mjs
      - backup-cluster.mjs
