# Included by compose.yaml, also usable independently without application .env.
services:
  cassandra:
    image: ${CARTYX_CASSANDRA_IMAGE:-ghcr.io/biozal/cartyx-cassandra@sha256:6d29c4203ab50b406d2bc0bacd5c7aea5cb1f98686df95459efd6c5677585b0a}
    entrypoint: [bash, /config/cassandra-start.sh]
    environment:
      CASSANDRA_CLUSTER_NAME: cartyx-local
      CASSANDRA_DC: dc1
      CASSANDRA_ENDPOINT_SNITCH: GossipingPropertyFileSnitch
      CASSANDRA_HOST: localhost
      MAX_HEAP_SIZE: 1G
      HEAP_NEWSIZE: 256M
    volumes:
      - cassandra-data:/var/lib/cassandra
      - ../charts/cartyx-data/files:/config:ro
      - ../../.local/data/local:/secrets:ro
    # CQL is internal only. Use exec for administration; no default-login window
    # is exposed on the host during initial provisioning.
    healthcheck:
      test: [CMD, python3, /config/cassandra-admin.py, ready]
      interval: 30s
      timeout: 35s
      retries: 5
      start_period: 180s
    mem_limit: 3g
    cpus: 2
    restart: unless-stopped

  cassandra-init:
    image: ${CARTYX_CASSANDRA_IMAGE:-ghcr.io/biozal/cartyx-cassandra@sha256:6d29c4203ab50b406d2bc0bacd5c7aea5cb1f98686df95459efd6c5677585b0a}
    entrypoint: [python3, /config/cassandra-admin.py, bootstrap]
    environment:
      CASSANDRA_HOST: cassandra
    volumes:
      - ../charts/cartyx-data/files:/config:ro
      - ../../.local/data/local:/secrets:ro
    depends_on:
      cassandra:
        condition: service_started
    mem_limit: 256m
    restart: 'no'

  janusgraph:
    image: ${CARTYX_JANUSGRAPH_IMAGE:-ghcr.io/biozal/cartyx-janusgraph@sha256:afa8a11d129f1cab44e5998565f92a950d916d2c94a677e8114849967372a375}
    entrypoint: [bash, /config/janusgraph-start.sh]
    environment:
      CASSANDRA_HOST: cassandra
    ports:
      - '127.0.0.1:${GREMLIN_LOCAL_PORT:-18182}:8182'
    volumes:
      - ../charts/cartyx-data/files:/config:ro
      - ../../.local/data/local:/secrets:ro
    depends_on:
      cassandra-init:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          CMD-SHELL,
          'java -Xms32m -Xmx128m -cp "lib/*" groovy.ui.GroovyMain /config/graph-probe.groovy >/dev/null 2>&1',
        ]
      interval: 60s
      timeout: 30s
      retries: 3
      start_period: 120s
    mem_limit: 2g
    cpus: 2
    restart: unless-stopped

volumes:
  cassandra-data:
