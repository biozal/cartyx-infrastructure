apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cartyx-data
spec:
  # Dev image upgrade, off-host restore and capacity checks passed.
  suspend: false
  interval: 5m
  timeout: 15m
  chart:
    spec:
      chart: deploy/charts/cartyx-data
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: cartyx-data-prod
        namespace: flux-system
      valuesFiles:
        - deploy/charts/cartyx-data/values.yaml
        - deploy/charts/cartyx-data/values-prod.yaml
  install:
    remediation:
      retries: 0
  upgrade:
    remediation:
      retries: 0
